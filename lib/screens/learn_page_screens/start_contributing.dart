import 'package:flutter/material.dart';

class StartContributing extends StatelessWidget {
  const StartContributing({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}